'use client'
import { useEffect,useState } from 'react'
import Link from 'next/link'
import { ArrowRight,FileUp } from 'lucide-react'
export default function UploadArtworkStart(){const[products,setProducts]=useState<Array<{id:string;name:string;description:string;images:Array<{url:string}>}>>([]);const[error,setError]=useState('');useEffect(()=>{void fetch('/api/products').then(async(response)=>{const payload=await response.json();if(!response.ok)throw new Error(payload.error?.message||'Products could not be loaded.');setProducts(payload.data.products)}).catch((caught)=>setError(caught.message))},[]);return <main className="min-h-[75vh] bg-zinc-50 px-4 py-12"><div className="mx-auto max-w-6xl"><div className="flex items-center gap-3"><FileUp className="h-8 w-8 text-primary"/><div><h1 className="text-3xl font-black">Upload your artwork</h1><p className="mt-1 text-muted-foreground">First choose the product. Its configured variants and production sizes will be used in the upload form.</p></div></div>{error&&<p className="mt-5 rounded bg-red-50 p-3 text-red-700">{error}</p>}<div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{products.map((product)=><Link key={product.id} href={`/products/${product.id}?uploadArtwork=1`} className="group overflow-hidden rounded-xl border bg-white"><div className="aspect-[4/3] bg-zinc-100">{product.images?.[0]?.url&&<img src={product.images[0].url} alt="" className="h-full w-full object-cover"/>}</div><div className="p-5"><h2 className="text-xl font-black">{product.name}</h2><p className="mt-2 line-clamp-2 text-sm text-muted-foreground">Select from this product’s real production sizes and variants.</p><span className="mt-4 inline-flex items-center gap-2 font-bold text-primary">Choose product <ArrowRight className="h-4 w-4"/></span></div></Link>)}</div></div></main>}
