import type { Metadata } from 'next'
import Link from 'next/link'
import { CheckCircle2,Package,ArrowRight } from 'lucide-react'
import { Button } from '@/components/ui/button'
export const metadata:Metadata={title:'Order Confirmation | Alibaba Signs',description:'Your Alibaba Signs payment confirmation.'}
export default async function OrderSuccessPage({searchParams}:{searchParams:Promise<{order?:string}>}){const{order}=await searchParams;return <main className="grid min-h-[75vh] place-items-center bg-background px-4 py-12"><div className="max-w-lg text-center"><CheckCircle2 className="mx-auto h-16 w-16 text-green-600"/><h1 className="mt-6 text-3xl font-black">Payment submitted</h1><p className="mt-3 text-muted-foreground">We are confirming your payment securely with Stripe. The verified webhook updates your order as soon as Stripe confirms it.</p>{order&&<p className="mt-5 rounded-lg bg-secondary p-4 font-mono font-bold">Order {order}</p>}<div className="mt-7 rounded-xl border bg-card p-6 text-left"><div className="flex gap-3"><Package className="mt-0.5 h-5 w-5 text-primary"/><div><h2 className="font-bold">What happens next</h2><p className="mt-1 text-sm text-muted-foreground">Your saved design and print-ready production assets are available to the production team through the order record.</p></div></div></div><div className="mt-7 grid gap-3 sm:grid-cols-2"><Link href="/account/orders"><Button variant="outline" className="w-full">View orders</Button></Link><Link href="/products"><Button className="w-full">Continue shopping <ArrowRight/></Button></Link></div></div></main>}
